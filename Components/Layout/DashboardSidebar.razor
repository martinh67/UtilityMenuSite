<div class="dashboard-sidebar bg-dark text-light d-flex flex-column p-3" style="min-width:220px; min-height:100vh;">
    <div class="mb-4 mt-2">
        <a href="/" class="text-decoration-none fw-bold text-primary fs-5">
            <i class="bi bi-grid-3x3-gap-fill me-2"></i>UtilityMenu
        </a>
    </div>
    <nav class="nav flex-column gap-1">
        <NavLink href="/dashboard" Match="NavLinkMatch.All"
                 class="nav-link d-flex align-items-center gap-2">
            <i class="bi bi-speedometer2"></i> Overview
        </NavLink>
        <NavLink href="/dashboard/licence" Match="NavLinkMatch.All"
                 class="nav-link d-flex align-items-center gap-2">
            <i class="bi bi-key"></i> Licence
        </NavLink>
        <NavLink href="/dashboard/billing" Match="NavLinkMatch.All"
                 class="nav-link d-flex align-items-center gap-2">
            <i class="bi bi-credit-card"></i> Billing
        </NavLink>
        <NavLink href="/dashboard/devices" Match="NavLinkMatch.All"
                 class="nav-link d-flex align-items-center gap-2">
            <i class="bi bi-laptop"></i> Devices
        </NavLink>
        <NavLink href="/dashboard/settings" Match="NavLinkMatch.All"
                 class="nav-link d-flex align-items-center gap-2">
            <i class="bi bi-gear"></i> Settings
        </NavLink>
    </nav>
    <AuthorizeView Roles="Admin">
        <div class="border-top border-secondary mt-3 pt-3">
            <a href="/admin" class="nav-link d-flex align-items-center gap-2" style="color:rgba(255,99,99,0.8);">
                <i class="bi bi-shield-lock"></i> Admin Panel
            </a>
        </div>
    </AuthorizeView>
    <div class="mt-auto pt-4 border-top border-secondary">
        <a href="/" class="nav-link text-secondary small">
            <i class="bi bi-arrow-left me-1"></i>Back to site
        </a>
    </div>
</div>

<style>
    .dashboard-sidebar .nav-link { color: rgba(255, 255, 255, 0.5); }
    .dashboard-sidebar .nav-link:hover { color: rgba(255, 255, 255, 0.85); }
    .dashboard-sidebar .nav-link.active { color: #fff; font-weight: 600; }
</style>
